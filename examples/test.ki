import src.json.{ Json, JNull, JBool, JNumber, JString, JArray, JsonField, make_field, stringify, json_object }
import std.list.{ Cons, Nil }

effect fn main() -> IO[void] {
    let field: JsonField = make_field("name", JString("Alice"))
    let person: Json = json_object(Cons(field, Nil))
    std.io.println(stringify(person))
    return
}
